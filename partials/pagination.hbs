<nav class="pagination" role="navigation">
  <div class="row">
    <div class="col-lg-4">
      {{#if prev}}
        <a class="pull-left" href="{{page_url prev}}"><i class="fa fa-arrow-left fa-fw"></i> Newer Posts</a>
      {{/if}}
    </div>
    <div class="col-lg-4 text-center">
      <span class="page-number">Page {{page}} of {{pages}}</span>
    </div>
    <div class="col-lg-4">
      {{#if next}}
        <a class="pull-right" href="{{page_url next}}">Older Posts <i class="fa fa-arrow-right fa-fw"></i></a>
      {{/if}}
    </div>
  </div>
</nav>
